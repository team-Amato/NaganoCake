<div class="container mt-5 mb-5">
  <div class="row">
    <div class="col-md-8 pt-3">
        <div class="col-md-12" style="background-color:#f5f5f5;">
        <h5>注文履歴一覧</h5>
        </div>
        <table class="table table-striped">
            <tr>
                <th>購入日時</th>
                <th>購入者</th>
                <th>注文個数</th>
                <th>注文ステータス</th>
            </tr>
            <tr>
                <td><%= @order.created_at.strftime('%Y/%m/%d') %></td>
                <td><%= @order.name %></td>
                <td><%= @order.count%></td>
                <td><%= @order.order_stutas %></td>
            </tr>
        </table>
        </div>
        </div>
    </div>
</div>
